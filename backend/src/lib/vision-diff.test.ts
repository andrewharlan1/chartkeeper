// uses vitest, not Jest — see deferred features
// The original tests use vitest imports (describe, it, expect, vi from 'vitest')
// which are incompatible with the Jest test runner used in this project.
// eslint-disable-next-line jest/no-disabled-tests
describe.skip('vision-diff (vitest-only)', () => { it('skipped', () => {}); });
